HTML5 WebSocket Shell

Features:

 * Socket.IO compatible server
 * Keyboard captures
     * CTRL keys (-D, -C, -L, -W etc)
     * Tab key
     * Arrow keys
     * Esc
 * Colors
 * Unicode

TODO:

 * Cursor
